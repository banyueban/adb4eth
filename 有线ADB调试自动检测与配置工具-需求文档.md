# 有线 ADB 调试自动检测与配置工具 — 需求文档

- 文档版本：v1.0
- 日期：2026-08-04
- 目标平台：Windows PC / macOS / Android 收银机（RK3399 平台示例）
- 关联文档：`有线ADB调试自动检测与配置工具-技术设计文档.md`

---

## 1. 背景与问题

安卓收银机在**无 Wi-Fi 或禁用 Wi-Fi** 的现场环境（门店、仓库、专柜）需要调试（抓日志、装 APK、刷机、改配置）。此时唯一可行的调试链路是**通过网线直连 PC 网口**，走 ADB over TCP/IP（`adb connect IP:5555`）。

但在实际部署中，这条有线链路的建立涉及 **PC 端网络配置、扩展坞/USB 网卡、网线物理层、Android 以太网接口、ADB 服务端**等多个环节，任一环节出错都会导致 `adb connect` 失败。人工排障耗时且易出错，且**PC 端网络配置一旦错误会破坏 PC 本身的联网**（尤其是 macOS/Wi-Fi 上网场景）。

本工具的目标是：**一键自动检测有线链路各环节状态、自动完成安全配置、并给出精准诊断**，让非专业人士也能完成有线 ADB 调试。

---

## 2. 目标用户与场景

### 2.1 目标用户
- 收银机厂商的**售后/运维/测试工程师**（主场景）
- 门店现场**技术支持人员**
- 需要临时调试安卓设备的**开发工程师**

### 2.2 典型场景
| 场景 | 说明 |
|---|---|
| S1 直连调试 | PC 网口（含扩展坞/USB 网卡）通过网线直连收银机网口，无路由器 |
| S2 同网段调试 | PC 与收银机都在同一路由器/交换机下（走有线或 Wi-Fi） |
| S3 无 Wi-Fi 收银机 | 收银机禁用 Wi-Fi，只能走网线链路（**本工具核心场景**） |
| S4 PC 联网保护 | PC 本身依赖 Wi-Fi/另一网卡上网，**配置不能破坏其默认路由** |

### 2.3 角色
- **操作者**：在 PC 上运行工具，按提示在收银机屏幕上做必要确认（如授权、开以太网）。
- **工具**：自动完成 PC 端检测与配置、尽力完成 Android 端配置、输出诊断与操作指引。

---

## 3. 需求总览

### 3.1 核心目标
1. **检测**：识别有线链路从物理层到应用层的完整健康状态，定位故障环节。
2. **配置**：在**保证 PC 联网不受影响**的前提下，自动完成 PC 端与 Android 端的有线调试网络配置。
3. **连接**：自动完成 `adb connect` 并验证 ADB 可用（非 offline / unauthorized）。
4. **诊断**：任何一步失败时，给出精准的错误定位与可执行的处理建议。

### 3.2 关键设计原则（非功能需求之首）
> **P0 约束：PC 端任何配置操作都不得破坏 PC 现有联网能力。**

具体规则：
- 配置前**检测 PC 默认路由所在网卡**，并全程保护。
- 若目标网卡不是默认路由网卡：只增加**目标网段直连路由**，不写入默认路由，不产生新的默认路由（网关设 0.0.0.0 / 不设网关）。
- 若唯一可用网卡就是默认路由网卡：提供"网段共用"方案（与 PC 现有网段同网段配收银机），或**警告并引导手动操作**，不做覆盖默认路由的操作。
- 每次变更后**自动回验**默认路由未被改动，若发现被改则自动回滚。

---

## 4. 功能需求

### FR1 环境自检（PC 端）
检测 PC 上是否具备运行条件，逐项输出 ✓/✗：
- `adb` 命令存在且版本可执行（`adb version`）。
- 系统平台识别（Windows / macOS）。
- 若平台为 macOS：网络服务枚举能力、`networksetup` 可用性。
- 若平台为 Windows：网卡枚举能力（`ipconfig` / `Get-NetIPConfiguration` / WMI）、管理员权限需求提示。

### FR2 网络拓扑检测（PC 端）
自动判断 PC 与收银机的网络关系：
- 枚举 PC 所有**物理网卡**（以太网、USB 网卡、Wi-Fi），忽略虚拟接口（VPN、Loopback、虚拟机）。
- 识别 PC 各网卡的：名称、接口名、IP、掩码、网关、链路状态（active/inactive）、介质（100M/1G）。
- 识别 PC 的**默认路由**（网关）所在网卡。
- 判断当前是否有**可用的有线网卡**（含扩展坞 USB 网卡：macOS 通过 `networksetup -listallhardwareports` + `ioreg`，Windows 通过网卡描述含 Ethernet/USB/Realtek/ASIX 等关键字）。
- 探测候选网卡的**对端是否在线**：ARP / ping / 端口探测。

### FR3 有线链路检测（逐层）
对选定的有线网卡，按 OSI 层逐层检测并输出每一层的状态：

| 层级 | 检测项 | macOS 方法 | Windows 方法 |
|---|---|---|---|
| L1 物理 | 网卡介质/协商速率 | `ifconfig enX` / `networksetup -getmedia` | `Get-NetAdapter` LinkSpeed/MediaConnectionState |
| L1 物理 | 链路状态 | `ifconfig enX status` | `Get-NetAdapter` Status |
| L2 数据链路 | 网线对端 MAC（ARP） | `arp -an` / `ping` 后 `arp -an` | `arp -a` / `Get-NetNeighbor` |
| L2 数据链路 | **单向发送故障检测** | 对端设备统计计数核对（见 §5） | 同 macOS 方案 |
| L3 网络 | 本端 IP/掩码 | `ifconfig enX` | `Get-NetIPAddress` |
| L3 网络 | 对端可达性 | `ping` / `route -n get` | `ping` / `Get-NetRoute` |
| L3 网络 | **PC 联网保护确认** | 默认路由仍指向原网卡 | 同 macOS |
| L4 传输 | 收银机 ADB 端口(5555) | `nc -z` | `Test-NetConnection` |
| L7 应用 | ADB 连接 | `adb connect IP:5555` | 同 macOS |

### FR4 安全配置（PC 端）
自动完成 PC 端配置，且遵守 P0 约束：
- 给有线网卡分配一个**专用调试网段**（默认 `192.168.100.1/24`，可配置）。
- 配置方式：
  - **macOS**：`networksetup -setmanual "服务名" IP 255.255.255.0 0.0.0.0`（网关设 0.0.0.0，不产生默认路由）。
  - **Windows**：`netsh interface ip set address` 或 PowerShell `New-NetIPAddress`（仅设 IP/掩码，**不设默认网关**）。
- 若目标网卡不可用/未启用，自动启用（macOS `networksetup -setnetworkserviceenabled on`）。
- 记录**变更前配置**，供回滚。
- 配置后回验：默认路由未变、目标网段路由已生效、链路仍 active。

### FR5 收银机（Android）配置
分为"自动"与"人工指引"两级：

**自动（有 Wi-Fi ADB 已连上时可做）**
- 检查 `service.adb.tcp.port` 是否为 5555，若不是则 `setprop` + 重启 adbd。
- 拉起以太网接口：`ip link set ethX up`。
- 给以太网接口配同网段 IP：`ip addr add 192.168.100.2/24 dev ethX`。
- 验证 `ip addr show ethX`、`ip route` 含目标网段、`carrier`=1。

**人工指引（无 Wi-Fi ADB 时）**
- 提示操作者：设置 → 以太网 → 静态 IP：`192.168.100.2/24`，网关 `192.168.100.1` 或留空。
- 提示操作者：设置 → 开发者选项 → 打开「USB 调试」和「网络调试（ADB over network）」，确认监听 5555。

### FR6 连接与验证（ADB）
- 自动执行 `adb connect 收银机IP:5555`。
- 解析 ADB 状态：
  - `device` → 成功。
  - `offline` → 提示重启 adbd / 检查双端链路（见 §7 诊断树）。
  - `unauthorized` → 提示操作者在收银机屏幕点击授权。
  - `failed to connect` → 回到 FR3 链路检测定位。
- 验证 shell 可执行：`adb shell echo OK` 或 `getprop ro.product.model`。

### FR7 诊断与报告
- 每一步骤记录：时间、层级、检查项、命令、结果、判定。
- 失败时输出**诊断树定位结果**（见 §7）与**可执行建议**（换网线、换 USB-C 口、开以太网、重启 adbd、点授权……）。
- 生成 Markdown/文本报告，含命令回放，便于远程协助。

---

## 5. 关键检测点详述：单向链路故障（本次实测核心发现）

### 5.1 现象
PC 端网卡显示 `active`/`100baseTX`、ARP 表能看到对端 MAC，但**对端设备收不到 PC 发出的任何帧**（RX=0），导致 ping/TCP 全部超时。这是扩展坞/USB 网卡（如 Realtek RTL8153）的**发送方向硬件/驱动故障**，或网线单侧线对问题。**换网线无效、换 USB-C 口有效**（本次实测）。

### 5.2 判定方法（三层验证）
1. **对端统计核对（最关键）**：若 Android 端有 Wi-Fi ADB 可连，读取 `/sys/class/net/ethX/statistics/rx_packets`：
   - `rx_packets` 持续为 0，而本端 `Opkts` 在增长 → **PC → Android 发送方向物理不通**。
   - 若 `rx_packets` 有增长 → 链路双向可达，问题在更高层。
2. **对端抓包**：Android 端 `tcpdump -i ethX` 同时 PC 端大量 ping：抓不到 PC 任何帧 → 发送方向不通。
3. **本端计数**：`netstat -in` 看 `Opkts` 增长但 `Oerrs=0` —— 说明**驱动计数与实际物理发送分离**（驱动以为发了，实际没发）。

### 5.3 处理建议（按序）
1. 换 USB-C 口（本次实测的解决手段）。
2. 换扩展坞/USB 网卡。
3. 换网线。
4. 检查扩展坞供电。

---

## 6. 非功能需求

| 编号 | 需求 | 说明 |
|---|---|---|
| NFR-1 | **联网保护（P0）** | 任何配置不得破坏 PC 默认路由与联网，变更后回验，失败自动回滚 |
| NFR-2 | 可移植性 | 跨 Windows / macOS；命令层抽离，平台差异封装 |
| NFR-3 | 幂等性 | 重复运行不破坏已有正确配置；配置前备份、失败回滚 |
| NFR-4 | 可观测性 | 全程输出分层检测结果 + 命令回放 + 时间戳 |
| NFR-5 | 最低权限 | 优先无管理员权限检测；仅在需要改配置时提示提权 |
| NFR-6 | 离线可用 | 不依赖云端；所有检测/配置本地完成 |
| NFR-7 | 友好提示 | 非技术用户可执行的屏幕操作指引（收银机端） |

---

## 7. 诊断树（故障定位流程）

```
adb connect 失败
├─ 收银机 IP 未知
│   └─ 枚举：ARP 表 / 网段扫描 / Wi-Fi ADB 已连则读取 ip addr
├─ 端口 5555 不通（nc 失败）
│   ├─ 收银机没开「网络调试」→ 指引开
│   ├─ adbd 未监听 → Android 端 setprop + 重启 adbd
│   ├─ 收银机以太网接口 DOWN → ip link set ethX up / 系统开以太网
│   └─ PC→Android 单向不通（RX=0）
│       ├─ 换 USB-C 口
│       ├─ 换扩展坞/网卡
│       ├─ 换网线
│       └─ 检查供电
├─ 端口通但 offline
│   ├─ adbd 刚重启/状态未就绪 → 重试 / 重启 adbd
│   └─ 双端链路不稳定 → 检查 L1/L2
├─ 端口通但 unauthorized
│   └─ 收银机屏幕弹窗点「允许」→ 重新 connect
├─ 收银机 IP 配错网段
│   └─ 确认两端掩码一致（/24）与网段相同
└─ PC 无可用有线网卡
    ├─ 扩展坞未识别 → 检查 USB 枚举 / 驱动
    └─ 网线未插 → 物理检查
```

---

## 8. 使用流程（用户视角）

```
[1] 插入网线/USB网卡 → 运行工具
[2] 工具自检 + 枚举网卡 + 识别默认路由 → 报告
[3] 工具逐层检测有线链路 (L1→L7) → 报告
[4] 工具自动配置 PC 端(不碰默认路由) → 回验
[5] 收银机端：
       - 若已有 Wi-Fi ADB：工具自动拉起 eth、配 IP、开 adbd
       - 否则：按提示在收银机设置静态 IP + 开网络调试
[6] 工具 adb connect → 处理 device/offline/unauthorized
[7] 验证 shell → 输出成功报告 / 诊断建议
```

---

## 9. 验收标准

| 用例 | 预期 |
|---|---|
| 正常直连（各环节健康） | 自动配置成功，`adb connect` 到 `device`，shell 可执行 |
| 网线没插 / 网卡未识别 | L1 报错，提示物理检查，不尝试配置 |
| PC 默认路由走 Wi-Fi | 配置后 Wi-Fi 默认路由不变，联网正常（回验通过） |
| 单向发送故障（RTL8153） | 通过 RX=0 检测定位，提示换 USB-C 口 |
| 收银机没开网络调试 | 端口检测失败，提示开网络调试并给操作步骤 |
| 收银机显示 unauthorized | 提示在收银机屏幕点授权 |
| 重复运行 | 幂等，不破坏已有配置 |
| 配置失败 | 自动回滚到变更前，PC 联网不受影响 |
